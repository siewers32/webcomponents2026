# New Css en Webcomponent tricks (2026)

* Start webserver met: `npx browser-sync start --server --files "index.html, **/*.css, **/*.js`
* Browser-sync wordt dan tijdelijk geinstalleerd (geen node-modules etc.)
